#include<iostream>
#include<cmath>
using namespace std;
int factor(int a,int b){
	if(a%b==0){
		cout<<"multiple of each other";
	}
	else
	cout<<"not multiple of each other";
}
main(){
	int a,b;
	cout<<" enter two numbers";
	cin>>a>>b;
	factor(a,b);
}
